name: Publish
on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+*'
jobs:
  publish:
    uses: sanzenwin/workflows/.github/workflows/pypi-release-publish-win.yml@main
    permissions:
      contents: write
    secrets: inherit
